<Window x:Class="FilConvWpf.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:z="clr-namespace:FilConvWpf"
        Title="Fil Converter" Height="350" Width="525" AllowDrop="True">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <Menu Grid.ColumnSpan="2">
            <MenuItem Header="_Файл">
                <MenuItem Header="_Открыть..." Click="menuOpen_Click" />
                <MenuItem Header="_Сохранить как..." Click="menuSaveAs_Click" />
                <Separator />
                <MenuItem Header="Вы_ход" Click="menuExit_Click" />
            </MenuItem>
            <MenuItem Header="_Справка">
                <MenuItem Header="_О программе..." Click="menuAbout_Click" />
            </MenuItem>
        </Menu>
        <z:Preview x:Name="left" Grid.Row="1" Margin="0 0 4pt 0" Title="Оригинал"/>
        <GridSplitter Grid.Row="1" Width="4pt"/>
        <z:Preview x:Name="right" Grid.Row="1" Grid.Column="1" Title="Результат"/>
    </Grid>
</Window>
